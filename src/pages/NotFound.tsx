import React from "react";

// Display this page under * in Routes and show it when URL can't be found
// Show simple message and link back to Home page
const NotFound = () => {

};

export default NotFound;